import em from '@$$emscript'
export const $U = em.$declare('COMPOSITE')

export function find(tool_name: string): string {
    return ''
}
//    var dir: string = ^^em$props.get^^("em.build.InstallDir." + toolName, null)
//    dir = ^^em$session.getToolsHome()^^ + "/" + toolName" if !dir
//    return dir if ^^$Fs.existsSync(dir)^^
//    printf "directory not found: %s\n", dir
//    fail
